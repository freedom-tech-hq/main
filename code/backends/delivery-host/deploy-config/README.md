This directory is always overridden.

- `./compose.yaml` overrides it with bind of `test-data/dms/config/`
- deployments override it with env-specific secrets physically in `deploy/workspace/` before build.
