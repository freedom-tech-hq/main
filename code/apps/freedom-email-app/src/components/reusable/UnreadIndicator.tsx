import { Box } from '@mui/material';
import React from 'react';

export const UnreadIndicator = () => (
  <Box className="primary-bg" sx={{ width: '6px', height: '6px', minWidth: '6px', maxWidth: '6px', borderRadius: '3px' }} />
);
