# Freedom Crypto Service

Defines two things:

- `UserKeys` interface - user keys enumerator.
- `decryptOneEncryptedValue` function - connects `freedom-crypto.decryptEncryptedValue()` with `UserKeys`.
