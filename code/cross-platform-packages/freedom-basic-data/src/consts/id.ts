/** The minimum length of an ID, not counting any common prefix. */
export const MIN_ID_LENGTH = 1;

/** The maximum length of an ID, not counting any common prefix. */
export const MAX_ID_LENGTH = 4096;
